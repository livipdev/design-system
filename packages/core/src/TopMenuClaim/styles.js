const styles = (theme) => ({
  root: {
    ...theme.alignments.autoMarginLeft,
    whiteSpace: theme.alignments.whitespaces.preWrap,
  },

  typography: {
    fontStyle: 'italic'
  }
});

export default styles;
