import { getHalfSpace } from '../styles/spacing';

const styles = (theme) => ({
  root: {
    display: 'flex',
    justifyContent: 'center',
    whiteSpace: 'pre-wrap',
    marginLeft: 'auto',
  },
});

export default styles;
