const guttersProps = {
  xs: 1,
};

const contentProps = {
  xs: 10,
};

const defaultProps = {
  gutters: guttersProps,
  content: contentProps,
  justify: 'center',
  alignItems: 'center',
};

export default defaultProps;
