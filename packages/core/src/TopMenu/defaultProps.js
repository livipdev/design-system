const containerProps = {
  xs: 12,
};

const claimProps = {
  xs: 9,
  justify: 'center',
  alignItems: 'center',
};

const socialProps = {
  xs: 1,
  justify: 'flex-end',
  alignItems: 'center',
};

const itemsProps = {
  claim: claimProps,
  social: socialProps,
};

const defaultProps = {
  container: containerProps,
  items: itemsProps,
  justify: 'center',
  alignItems: 'center',
};

export default defaultProps;
