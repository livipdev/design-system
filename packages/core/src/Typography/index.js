import React from 'react';
import MuiTypography from '@material-ui/core/Typography';

const Typography = (props) => <MuiTypography {...props} />;

Typography.propTypes = MuiTypography.propTypes;

export default Typography;
