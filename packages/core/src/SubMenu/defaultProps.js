const menuProps = {
  xs: 8,
  justify: 'center',
  alignItems: 'center',
};

const buttonsProps = {
  xs: 2,
  justify: 'flex-end',
  alignItems: 'center',
}

const childrenProps = {
  menu: menuProps,
  buttons: buttonsProps,
};

const defaultProps = {
  children: childrenProps,
  justify: 'center',
  alignItems: 'center',
};

export default defaultProps;
